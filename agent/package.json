{
  "name": "pi-coworker-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Bot Agent 守护程序：飞书内交互的常驻 agent（local=个人本机 / server=公司共享）",
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "start": "node src/index.ts",
    "daemon": "node bin/coworker-daemon.ts",
    "self-test": "node scripts/self-test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
