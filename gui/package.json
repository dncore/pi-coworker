{
  "name": "pi-coworker-gui",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "check": "cd backend && npx tsc -p tsconfig.json",
    "dev": "cd src-tauri && cargo run",
    "build": "cd src-tauri && cargo tauri build",
    "icons": "node scripts/gen-icon.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
